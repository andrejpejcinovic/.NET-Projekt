@model IEnumerable<ProgramskoInzenjerstvo.Models.PosiljkaStanje>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Stanje)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Stanje)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDPosiljkaStanje }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDPosiljkaStanje }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDPosiljkaStanje })
        </td>
    </tr>
}

</table>
