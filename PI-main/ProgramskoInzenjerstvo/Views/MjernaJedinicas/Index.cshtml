@model IEnumerable<ProgramskoInzenjerstvo.Models.MjernaJedinica>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MjernaJedinica1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MjernaJedinica1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDMjernaJedinica }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDMjernaJedinica }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDMjernaJedinica })
        </td>
    </tr>
}

</table>
